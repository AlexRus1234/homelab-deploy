# Архитектура и Сетевая Топология

Данный документ описывает общую архитектуру домашнего дата-центра (HomeLab), схему взаимодействия узлов

## 1. Общая топология узлов

В инфраструктуре задействованы логические и физические узлы, выполняющие строго разделенные функции.

### 1.1. SHLZ00 (Шлюз / Маршрутизатор)
*   **Роль:** Главный шлюз, DNS-сервер и Reverse Proxy домашней сети.
*   **ОС:** Arch Linux (на базе специализированного мини-ПК с многопортовой сетью).
*   **Сервисы:** Firewall (nftables), DNS (Unbound/Dnsmasq), Step-CA (удостоверяющий центр, выдаёт TLS-сертификаты для локальных сервисов; корневый сертификат раздаётся по `http://172.20.0.1:9000/roots.pem`).
*   **Функция:** Терминирует входящий трафик, управляет маршрутизацией, выдает локальные доменные имена (например, `*.yadr00.internal`) и проксирует запросы на нужные порты виртуальных машин.

### 1.2. SKLD00 (NAS / СХД)
*   **Роль:** Центральное хранилище данных и СУБД.
*   **Сервисы:** PostgreSQL, RUSTFS (S3-совместимое объектное хранилище).
*   **Сеть:** Подключен к общей сети маршрутизатора (LAN) и имеет выделенный сетевой интерфейс для прямого соединения с узлом `YADR01`. S3-endpoint RUSTFS доступен на P2P-линке по адресу `172.21.6.249:9000`.

### 1.3. YADR00 -> ВМ `obshaga`
*   **Роль:** Вычислительный узел для легковесных пользовательских приложений.
*   **ОС:** Arch Linux (минимальная установка).
*   **Сеть:** Подключена к общей сети (`vmbr0`). Доступ к сервисам возможен как через прокси (доменное имя), так и напрямую по IP ВМ и открытому порту.
*   **Размещенные сервисы:** 
    *   Homepage (Стартовый дашборд, заменил Dashy)
    *   LinkStack (Мультиссылки)
    *   Memos (Заметки)
    *   Webhook (Агент GitOps-синхронизации)

### 1.4. YADR01 -> ВМ `panelka`
*   **Роль:** Инфраструктурный вычислительный узел. Обрабатывает тяжелый  трафик 
*   **ОС:** Arch Linux (минимальная установка).
*   **Сеть:** Имеет два сетевых интерфейса:
    1.  Подключение к общей сети (`vmbr0`) для управления и приема входящих запросов.
    2.  Прямой L2-линк (Point-to-Point) до узла `SKLD00` (`vmbr1`) с пропускной способностью 2.5G.
*   **Размещенные сервисы:**
    *   NORA (Pull-through кэш-прокси для npm, go, oci-образов).  
    *   Khrazhevnik (кеш-прокси и зеркало linux-репозиториев: apt, rpm, pacman, apk, nix; RustFS + PostgreSQL по P2P-линку).
    *   Webhook (Агент GitOps-синхронизации)
*   **Обоснование архитектуры:** Кэширование слоев Docker-образов и языковых пакетов генерирует массированный трафик между прокси-сервером и S3-хранилищем. Размещение сервиса NORA на ВМ `panelka` позволяет направить этот трафик по выделенному аппаратному линку напрямую в NAS, полностью исключая нагрузку на основной коммутатор домашней сети.

### 1.5. LXC-контейнеры (вспомогательные сервисы)
На узлах `YADRXX` помимо ВМ разворачиваются легковесные LXC-контейнеры под вспомогательные сервисы, не вписывающиеся в модель Rootless Podman. Управляются они отдельными workflow (`yadr01-deploy-caddy.yml`, `yadr01-deploy-sftpgo.yml`) по SSH от пользователя `root` и используют общий секрет `SSH_PRIVATE_KEY` (в отличие от ВМ-деплоев с гранулярными ключами).

*   **Caddy** — Reverse Proxy. Живёт в собственном LXC на каждом узле `YADRXX` (своё инстанс на узел: `172.20.5.3` для yadr00, `172.20.6.2` для yadr01). Эталонные `Caddyfile` хранятся в `system-templates/caddy/Caddyfile.yadr00.template` и `Caddyfile.yadr01.template`. Детально доменная схема и пробросы описаны в `04_доменная_схема.md`.
*   **SFTPGo** — SFTP/FTP-сервер, LXC на `YADR01` (`172.20.6.8`).

### 1.6. Сетевая адресация

Основная сеть — `172.20.0.0/16` (2.5G). Адресация строится по принципу: **3-й октет** — номер узла (со смещением по типу устройства), **4-й октет** — отдельный контейнер/ВМ на узле. Смещения: SHLZ00 = 0, YADR00 = +5, RUKA00 = +35 и т.д.

#### Система именования узлов (Hostname)

| Префикс | Тип устройства | Примечание |
| :--- | :--- | :--- |
| **SHLZXX** | Сетевое оборудование | 00–09: роутеры; 10–29: точки доступа; 30–99: умные коммутаторы |
| **YADRXX** | Основные вычислительные серверы (x86) | Привязка к 3-му октету IP |
| **RUKAXX** | Мощные рабочие станции RISC-V / ARM | Привязка к 3-му октету IP |
| **SKLDXX** | Сетевые хранилища (NAS) | SKLD00 — узел с ленточной библиотекой |
| **ISKRXXAAA** | Маломощные серверы и помощники | AAA = 4-й октет IP |
| **EDINXXAAA** | Микро-ПК, узлы будущего кластера | AAA = 4-й октет IP |
| **PECHXX** | Серверы высокой производительности | Максимальная вычислительная мощность |
| **UPRVAAXX** | Модули управления (IPMI / BMC / iDRAC) | AA и XX = номер сервера |

#### Сегментация `172.20.0.0/16`

| Диапазон | Назначение |
| :--- | :--- |
| `172.20.0.xx` | Роутеры и коммутаторы (SHLZ) |
| `172.20.1.xx` | ПК и ноутбуки (провод) |
| `172.20.2.xx` | ПК и ноутбуки (WiFi) |
| `172.20.3.xx` | Телефоны и планшеты |
| `172.20.4.xx` | Принтеры и офисная техника |
| `172.20.5.xx` – `172.20.29.xx` | Серверы YADR (без Proxmox) |
| `172.20.30.xx` – `172.20.34.xx` | Серверы YADR (с Proxmox) |
| `172.20.35.xx` – `172.20.49.xx` | RUKAXX (мощные RISC-V/ARM ПК) |
| `172.20.50.xx` – `172.20.54.xx` | SKLDXX (NAS) |
| `172.20.55.xx` – `172.20.79.xx` | ISKRXX (маломощные серверы/помощники) |
| `172.20.80.xx` – `172.20.89.xx` | VPN |
| `172.20.90.xx` – `172.20.99.xx` | PECHXX (мощные серверы) |
| `172.20.100.xx` | Резерв EDINXX (будущий кластер) |
| `172.20.200.xx` – `172.20.209.xx` | Самодельный IoT (ESP32 и т.д., только LAN) |
| `172.20.250.x` – `172.20.254.x` | Loopback серверов и сетевых устройств |
| `172.20.255.2` – `172.20.255.254` | DHCP (динамика) |

> **Примечание:** в ближайшее время текущие узлы `YADR00` и `YADR01` будут перестроены **без Proxmox** — их адресация переедет в диапазон `172.20.5.xx` – `172.20.29.xx` (YADR без Proxmox). Диапазон `172.20.30.xx` – `172.20.34.xx` (YADR с Proxmox) останется под гипервизоры.

Дополнительные подсети:
*   `172.21.XX.0/24` — прямые линки (P2P), соотносятся с `172.20.0.0/16` по устройствам (например, `172.21.6.0/24` — SKLD00 ↔ YADR01).
*   `172.22.0.0/16` — IoT / гостевая сеть (нет доверия).
*   `172.23.XX.YY/30` и `/29` — безроутерное соединение, соотносится с `172.20.0.0/16`.
*   `172.24.0.0/16` — IPMI / BMC / iDRAC (UPRVAAXX).
*   `192.168.99.0/24` — Rescue-порт роутера SHLZ00 (аварийный доступ, `192.168.99.1`).

#### План адресации прямого линка `172.21.6.0/24` (SKLD00 ↔ YADR01, `vmbr1`)

Сеть выбрана `/24` (вместо первоначальной `/31`) для удобства подключения LXC/ВМ узла YADR01 к прямому линку. Сервисы на линке различаются **портами**, а не отдельными IP.

| Адрес | Узел | Примечание |
| :--- | :--- | :--- |
| `172.21.6.249` | SKLD00 | Хост. Сервисы по портам: PostgreSQL `:5432`, RustFS (S3) `:9000` |
| `172.21.6.250` | YADR01 | Хост (`vmbr1`) |
| `172.21.6.251` | LXC SFTPGo | Второй интерфейс (eth1) на `vmbr1`, SFTP `:2022` |
| `172.21.6.230` | ВМ `panelka` | NORA / Khrazhevnik → RustFS и PostgreSQL по линку |

> Прямого линка **пока не имеют**: ВМ git-runner, LXC caddy / pbs / synapse / bridge-matrix (доступ только через `vmbr0`; будут подключены к `vmbr1` позже).

#### Известные адреса узлов/сервисов

| Узел / сервис | IP-адрес | Назначение |
| :--- | :--- | :--- |
| SHLZ00 (Step-CA) | `172.20.0.1:9000` | Раздача корневого сертификата (`/roots.pem`), ACME-directory |
| Proxmox VE (yadr01) | `172.20.6.1:8006` | Web UI гипервизора |
| LXC Caddy (yadr00) | `172.20.5.3` | Reverse Proxy на узле yadr00 |
| LXC Caddy (yadr01) | `172.20.6.2` | Reverse Proxy на узле yadr01 |
| PBS (yadr01) | `172.20.6.3:8007` | Proxmox Backup Server |
| LXC SFTPGo (yadr01) | `172.20.6.8` | SFTP/FTP-сервер (Web UI: `:8080`, SFTP: `:2022`) |
| LXC synapse (yadr01) | `172.20.6.5` | Matrix Synapse (matrix.yadr01.internal) |
| LXC bridge-matrix (yadr01) | `172.20.6.6` | Matrix-мост |
| ВМ `obshaga` | `172.20.5.17` | Пользовательские сервисы (yadr00) |
| ВМ `panelka` | `172.20.6.9` | Инфраструктурные сервисы, NORA (yadr01) |
| ВМ `gitrunner` | `172.20.6.7` | Forgejo Runner — CI/CD, rootless Podman (yadr01), см. [09 — Forgejo](../Настройка/09_Forgejo_Git_CI.md) |
| SKLD00 (RUSTFS S3, P2P) | `172.21.6.249:9000` | S3-хранилище по выделенному линку `vmbr1` |

> Полная карта адресации серверов хранится отдельно (журнал «Сеть-адреса»). Типы узлов — см. таблицу «Система именования узлов» выше. Паспорта узлов: [SHLZ00 — Роутер](../Узлы/SHLZ00_роутер.md), [YADR00 — Proxmox](../Узлы/YADR00_Proxmox.md), [YADR01 — Proxmox](../Узлы/YADR01_Proxmox.md), [SKLD00 — NAS](../Узлы/SKLD00_NAS.md).

### 1.7. Доменная схема
Локальные зоны `*.yadrXX.internal` обслуживаются DNS-сервером SHLZ00 и резолвятся на IP локального инстанса Caddy соответствующего узла (`172.20.5.3` для yadr00, `172.20.6.2` для yadr01). Архитектура TLS (Step-CA/ACME) и конфигурация Caddy описаны в [`04_доменная_схема.md`](04_доменная_схема.md). Полный список доменов с IP:port и методом управления — [`05_доменные_имена.md`](05_доменные_имена.md).

---

Внутри виртуальных машин все приложения запускаются от бесправного пользователя (`app-runner`). По соображениям безопасности и во избежание конфликтов портов на хосте, все опубликованные порты (`PublishPort`) вынесены в нестандартный диапазон `10000+`. Нумерация портов ведется независимо для каждой виртуальной машины. Актуальный список сервисов и их портов — в [`05_доменные_имена.md`](05_доменные_имена.md).

---

## 3. Модель доставки конфигураций (GitOps)

Управление состоянием серверов осуществляется по Pull-модели.

1.  Все изменения в конфигурационных файлах (Systemd Quadlets) фиксируются в репозитории `homelab-deploy`.
2.  При выполнении коммита (Push) центральный Git-сервер (Forgejo) отправляет POST-запрос на порт `9000` целевой ВМ (`obshaga` или `panelka`).
3.  Сервис `webhook` на целевой ВМ верифицирует криптографическую подпись запроса.
4.  В случае успешной верификации локальный агент выполняет `sparse-checkout` (скачивание только директории, относящейся к данному серверу), обновляет файлы `.container` и отдает команду Podman на применение изменений.

Подробное описание работы механизма синхронизации описано в файле `03_gitops_агент.md`.